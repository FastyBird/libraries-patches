--- /dev/null
+++ ../src/Lazy/LazyUuidFromString.php
@@ -569,4 +569,11 @@
         return $this->unwrapped = (new UuidFactory())
             ->fromString($this->uuid);
     }
+
+    public static function __set_state(array $args): UuidInterface
+    {
+        return (new UuidFactory())
+            ->fromString($args['uuid']);
+    }
+
 }
